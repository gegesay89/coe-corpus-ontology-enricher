# Migration framework placeholder

v0 has no database and cannot write to one. This directory reserves the
append-only migration boundary for the Phase 2 immutable run substrate. The
database engine/version, migration tool, ownership model, rollback policy, and
backup/WAL exclusions must be approved before the first migration is added.

